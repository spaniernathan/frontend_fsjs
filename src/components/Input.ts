import styled from 'styled-components';

const Input = styled.input`
    border: none;
    border-bottom: 1.5px solid rgba(103, 104, 103, 0.5);
    background-color: #E8E8E8;
    border-radius: 0px;
`;

export default Input;
