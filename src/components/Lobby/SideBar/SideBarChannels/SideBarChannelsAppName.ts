import SideBarChannel from 'containers/Lobby/SidebarChannel';
import styled from 'styled-components';

const SideBarChannelsAppName = styled.div`
    background-color: #313338;
    line-break: auto;
    margin-top:auto;
    padding: 3vh;
    color: white;
    border-top: 4px solid rgba(0,0,0, 0.3);
`;

export default SideBarChannelsAppName;
